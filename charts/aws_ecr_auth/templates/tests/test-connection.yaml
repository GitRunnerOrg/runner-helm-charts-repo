apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aws_ecr_auth.fullname" . }}-test-connection"
  labels:
    {{- include "aws_ecr_auth.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aws_ecr_auth.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
